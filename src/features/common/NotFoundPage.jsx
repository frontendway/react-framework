import React from 'react'
import { connect } from 'react-redux'

class NotFoundPage extends React.Component {
  render() {
    return <div className="examples-NotFoundPage">
      NotFoundPage
    </div>
  }
}

export default connect(null, null)(NotFoundPage)
